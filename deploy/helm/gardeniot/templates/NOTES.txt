GardenIoT is installed in namespace {{ .Release.Namespace }} using the {{ .Values.transport }} transport.

Watch the loop:
  kubectl -n {{ .Release.Namespace }} logs -f deploy/{{ include "gardeniot.fullname" . }}-controller
  kubectl -n {{ .Release.Namespace }} logs -f statefulset/{{ include "gardeniot.fullname" . }}-device
{{- if and .Values.ui.enabled (eq .Values.transport "mqtt") }}

The garden page:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "gardeniot.fullname" . }}-ui 8088:8080
  open http://localhost:8088
{{- end }}
{{- if .Values.monitoring.enabled }}

Dashboards:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "gardeniot.fullname" . }}-grafana 3000:3000
  open http://localhost:3000/d/gardeniot
{{- end }}
